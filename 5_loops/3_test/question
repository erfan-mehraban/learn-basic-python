پاسخ پرسش‌های چند گزینه‌ی زیر رو به هر فرمتی که دوست دارید به صورت یک فایل پی دی اف آپلود کنید!

راهنمایی: در استفاده از پایتون آزادید!

1. کدام یک از تکه کد‌های زیر عددی را که از کاربر گرفته است چک می‌کند که رقم یکان آن 0 است؟

```
a = input()
1. if a%10=0:
2. if int(a)%10=0:
3. if int(a)%10==0:
4. if int(a%10)==0:
	print("ok")
```


2. کدام کد تا ابد اجرا می‌شود؟

```
a = 5
1.
while a<100:
	a += 3
2. while a>0:
	a -= 4
3. while a!= 4:
	a -= 2
4. while True:
	break
```


3. کد زیر چه چیزی چاپ می‌کند؟

```python3
a = 20
for i in range(20):
	if (a-i)%5==0:
		continue
	print(a-i)
```

