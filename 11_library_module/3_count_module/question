ماژولی بنویسید به نام ```counter```
این ماژول دستوری دارد به نام ```count```
همچنین دستوری دارد به نام ```how_much```  که تعداد بارهایی که دستور ```count``` صدا زده شده است را برمیگرداند.

یعنی ماژول شما باید به این صورت استفاده شود:

``` python3
from counter import count, how_much
count()
for i in range(10):
    count()
print(how_much())
# out: 11
```
