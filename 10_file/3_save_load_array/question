تابعی بنویسید که ۲ ورودی بگیرد: یک رشته و یک آرایه. سپس در فایلی به نام رشته‌ی ورودی، آرایه را ذخیره کند. نام این تابع را save بگذارید.

همچنین تابعی بنویسید به نام load که تنها یک رشته ورودی بگیرد و آرایه‌ای که در فایلی به نام رشته‌ی ورودی ذخیره شده است را برگرداند.

تمامی عناصر در آرایه یا عدد صحیح هستند یا رشته هستند.

نمره‌ی امتیازی (!) : تابع خود را به گونه‌ای بنویسید که لیست‌های تو در تو را نیز پشتیبانی کند.

# مثال

```
>>> save("first_array", [1, 6, "salam", 0])
```

```
>>> load("first_array")
[1, 6, "salam", 0]
```

